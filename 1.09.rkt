#lang racket
(define (+ a b)
  (if (= a 0)
      b
      (inc (+ (dec a) b))))
; линейно-рекурсивный процесс:
; (+ 4 5)
;   (+ 3 5)
;     (+ 2 5)
;       (+ 1 5)
;         (+ 0 5) => 5
;       (inc 5) => 6
;     (inc 6) => 7
;   (inc 7) => 8
; (inc 8) => 9

(define (+ a b)
  (if (= a 0)
      b
      (+ (dec a) (inc b))))
; линейно-итеративный процесс:
; (+ 4 5)
;   (+ 3 6)
;     (+ 2 7)
;       (+ 1 8)
;         (+ 0 9) => 9